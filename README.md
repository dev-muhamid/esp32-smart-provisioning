# esp32-smart-provisioning
ESP32 Smart Provisioner A robust, modular connectivity framework for the ESP32 using ESP-IDF v5.1. This project provides a "fail-safe" onboarding experience by allowing users to configure Wi-Fi credentials via a Web Portal (SoftAP) or Bluetooth Low Energy (NimBLE).
